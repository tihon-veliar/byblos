{
  "name": "byblos",
  "version": "0.0.1",
  "main": "main.js",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run src --pool threads",
    "test:watch": "vitest src --pool threads",
    "test:ui": "vitest --ui",
    "sandbox": "npx tsx --env-file=./.env src/sandbox/local-run.ts",
    "sandbox:mvp": "npx tsx src/sandbox/mvp-sandbox.ts",
    "sandbox:llm": "npx tsx --env-file=./.env src/sandbox/llm-sandbox.ts"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "esbuild": "^0.28.0",
    "obsidian": "latest",
    "typescript": "^5.0.0",
    "vitest": "^4.1.3"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.52",
    "ai": "^6.0.153"
  }
}
